
const Sampleexample =(prop)=>{
    const {Source,alt,height,width} = prop 
    return( 
     <img src={Source} alt={alt} height={height} width={width}/>  
      
    )
}
export default Sampleexample;